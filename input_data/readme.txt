_v2 files feature simplified geometries (single polygons with filled holes) to ease querying the GBIF api

bb contains bounding boxes which seems to work better than the _v2 geometries. bounding boxes can also be taken directly from deims.org
